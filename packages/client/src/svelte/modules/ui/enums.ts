export enum UI {
    LOADING,
    SPAWNING,
    READY, // Main player UI
    ESCAPED // Player has minted an exit NFT and left the pod
}

export enum TABS {
    POD,
    INBOX,
    ORDERS,
    SHOP,
    CHAT
}